# docs/SCHEMA.md 및 TASKS.md C-2 규약
# 이 파일을 .env로 복사해서 값을 채운다. .env는 커밋하지 않는다.

# Supabase 프로젝트 설정 > API 에서 확인
VITE_SUPABASE_URL=
# anon(public) key만 사용한다. service_role key는 절대 클라이언트에 넣지 않는다.
VITE_SUPABASE_ANON_KEY=

# BE 준비 전 mock으로 개발할 때 true. 값이 비어 있어도 자동으로 mock 모드가 된다.
VITE_USE_MOCK=true
